# PoloDeck Core — managed by the installer / first-run wizard.
# Coaches should use the browser setup screen, not this file.

NODE_ENV=production
PORT=8080
HOST=127.0.0.1
POLODECK_UI_PORT=8080
POLODECK_REQUIRE_SETUP=true
POLODECK_VERSION=0.1.0

# Filled by installer:
# POLODECK_WEB_ROOT=
# POLODECK_CONFIG_ENV_PATH=
# POLODECK_SESSION_SECRET=
# DATABASE_URL=
